# 🔍 VARIABLES COMPLETAS QUE USA EL BOT

## Variables del bot principal (bot.js):
- `DISCORD_BOT_TOKEN` ✅
- `BOT_PREFIX` ✅ 
- `BOT_OWNER_ID` ✅
- `SUPPORT_CHANNEL_ID` ✅
- `DESCUBRIMIENTOS_CHANNEL_ID` ✅
- `IMPLEMENTACIONES_CHANNEL_ID` ✅
- `CHAT_CHANNEL_ID` ✅
- `CMD_CHANNEL_ID` ✅
- `ANTICHEAT_WEBHOOK_URL` ✅
- `COMMUNITY_STEALTH_URL` (o `COMMUNITY_SERVER_INVITE`) ✅

## Variables de módulos:
- `MINIMAX_API_KEY` (para IA y TTS) ✅
- `GITHUB_TOKEN` (para repository_connector) ✅

## Variables opcionales que también usa:
- `MINIMAX_BASE_URL` (para IA avanzada)
- `MINIMAX_MODEL` (para IA avanzada)

---

**Dime cuáles de estas ya tienes configuradas en Railway, y te doy solo las que FALTAN.**